{
   "version": "2.0.0",
   "tasks": [
      {
         "type": "shell",
         "label": "install and compile",
         "command": "cd server && cargo build && cd ../clients/vs-code && pnpm i && pnpm compile"
      }
   ]
}
